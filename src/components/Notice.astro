---
interface Props {
  text: string;
}
const { text } = Astro.props;
---

{
  text.trim() && (
    <div class="bg-berry-600 text-white" role="status">
      <p class="mx-auto max-w-6xl px-4 py-2.5 text-center text-sm font-bold sm:px-6">{text}</p>
    </div>
  )
}
